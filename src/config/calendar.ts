/**
 * Paste your Calendly (or other calendar) embed code here.
 * The CalendarEmbed component will render it in a centered container.
 * If you have an iframe src only, we can switch to an iframeSrc prop instead.
 */
export const CALENDAR_EMBED_HTML = `
  <!-- Paste your Calendly/embed code here -->
`;
